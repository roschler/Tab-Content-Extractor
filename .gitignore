# chrome extension generator GITIGNORE elements
node_modules
temp
.tmp
dist
.sass-cache
app/bower_components
test/bower_components
package

# Our usual GITIGNORE elements
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
# Including these now that the build is no longer public, so that I don't have to do
#  npm install on remote installs.
#
web-site/node_modules/
web-site/jspm_packages/
solidity/node_modules/
solidity/jspm_packages/
solidity/build/

# Typescript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# ROS: Imported AngularJS ignores from PubNub AngularJS tutorial.
# Including these now that the build is no longer public, so that I don't have to do
#  npm install on remote installs.
#
.DS_Store
# node_modules
react/node_modules
react-native/node_modules
angular/flowtron/electron/node_modules
.idea
.idea*

# Don't commit the Browserified file.
#  YES, commit them so we can use them in an emergency.
# web-site/public/javascripts/browserified-files/

# Don't add public test directory, large files in there.
server/public/audio/test/

# Don't add large video files.
server/public/video/git-ignore/

# Don't add the dev-assets directory
server/public/dev-assets/

# Don't add the diretory for temporary images
server/image-temp/

# Don't include the AI model data directory.
models
